DATABASE_URL=postgresql://your-azhar-ai-db-connection-string
JWT_SECRET=pick-a-long-random-string-here
ANTHROPIC_API_KEY=your-anthropic-api-key
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"...paste the full downloaded JSON here as one line..."}
PORT=3000

# SECURITY NOTE: this file is a TEMPLATE only — it must never contain real
# values. Real secrets are set directly in Render's Environment tab, never
# committed to GitHub. If you ever create a real .env file locally, make
# sure it is listed in .gitignore so it cannot be accidentally uploaded.
